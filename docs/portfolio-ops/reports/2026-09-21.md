# Daily Portfolio Ops — 2026-09-21

## Status

`BLOCKED`

## Việc đã chọn + lý do

Không bắt đầu một lifecycle step của portfolio project vì preflight phát hiện xung đột giữa prompt vận hành và source of truth hiện tại.

- `VERIFIED` — README hiện tại nói standalone top-level `showcase/` đã bị loại bỏ; `/showcase` trên `main` trả 404.
- `VERIFIED` — không có `STATE.md` hay Coverage Matrix tồn tại trước run này, nên không có project dở để tiếp tục và không thể chứng minh điều kiện Rule D.
- `VERIFIED` — PR #18 đang mở nhưng không thuộc `auto/*`; branch của PR diverged, 17 commits behind `main`, GitHub báo non-mergeable. CI của head commit đã success. Không sửa PR này để tránh đè lên công việc hiện hữu.

Theo evidence policy, tự chọn `ngh1aa-repositories-redesign` khi path đó không còn tồn tại hoặc tự tuyên bố mọi project ≥4/5 đều là sai. Vì vậy run này chỉ bootstrap state/evidence ledger cần thiết cho lần chọn việc kế tiếp.

## Thay đổi thực chất

- Tạo `docs/portfolio-ops/STATE.md` với trạng thái và next action có evidence.
- Tạo `docs/portfolio-ops/COVERAGE.md` theo nguyên tắc không có bằng chứng = 0.
- Tạo báo cáo run này.
- Không sửa `uiux-factory/`, `skills_UIUX/`, `MetaGPT/`, legacy code, target project hay PR #18.

## Verification

- `VERIFIED` — `AGENTS.md` xác nhận source-of-truth order, evidence states, root-cause repair và Definition of Done.
- `VERIFIED` — `README.md` xác nhận `uiux-factory/` là canonical source, `runs/`/`generated/` là ignored output, và standalone `showcase/` đã bị removed.
- `VERIFIED` — `PROJECT-CONTEXT.template.md` yêu cầu UNKNOWN thay vì đoán và cấm bịa validation/outcome.
- `VERIFIED` — prototype checklist yêu cầu 3 style adjectives, hero moment, anti-AI look, component states, responsive và self-critique.
- `VERIFIED` — `THIRD_PARTY_NOTICES.md` hiện ghi Anthropic Skills và Motion Primitives; run này không sử dụng code/component bên ngoài nên không cần thêm notice.
- `VERIFIED` — open PR search: PR #18 tồn tại; không có open `auto/*` PR; branch search không có `auto/*` branch trước khi branch của run này được tạo.
- `VERIFIED` — CI head của PR #18: UIUX Factory CI run #257 completed `success`.
- `UNKNOWN` — local `git status` không thể được chứng minh bằng connector-only GitHub access.
- `UNKNOWN` — active Factory run lock ngoài GitHub không có runtime evidence trong run này.
- `NOT RUN` — compile/lint/test/build/browser QA, vì không có product/runtime code hoặc UI nào được thay đổi.

## Coverage Matrix trước / sau

- Trước: `UNKNOWN` — không có persisted matrix.
- Sau: 10/10 areas được khởi tạo ở `0/5` / `UNKNOWN`, đúng quy tắc “không có bằng chứng = 0”. Không có điểm nào được nâng bằng inference.

## Nguồn cảm hứng + license

Không dùng nguồn cảm hứng nào trong run này. Không copy code, component, layout, palette hay animation. Không có thay đổi `THIRD_PARTY_NOTICES.md`.

## Rủi ro còn lại

1. Selection rule C đang tham chiếu một `showcase/` không còn tồn tại theo README hiện tại.
2. Coverage chưa có project-specific evidence, nên chưa thể chọn Rule D một cách trung thực.
3. PR #18 đang diverged/non-mergeable và cần được xử lý độc lập, không nên bị Daily Portfolio Ops ghi đè.
4. Chưa có runtime evidence về Factory run lock.

## VIỆC CẦN CHỦ REPO LÀM/DUYỆT

1. Review draft PR của run này và xác nhận hướng xử lý mismatch `showcase/` nếu muốn thay đổi selection policy.
2. Không merge PR #18 chỉ dựa trên CI xanh; branch đang diverged/non-mergeable và cần reconciliation riêng.
