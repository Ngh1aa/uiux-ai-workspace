# Stack scope

Default stack target: GitHub Actions + Playwright Test + @axe-core/playwright + Lighthouse CI + Playwright screenshots, with Sharp only if media processing is actually integrated. Avoid overlapping community tools until a verified gap remains.
