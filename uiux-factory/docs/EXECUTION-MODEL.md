# Execution model

Local heavyweight inference is out of scope for the intended collaboration model. GitHub Actions is the preferred execution plane for repeatable QA and evidence generation. Remote AI reasoning remains outside the repository unless an explicitly configured API provider is needed for unattended automation.
